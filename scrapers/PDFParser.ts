// scrapers/PDFParser.ts
import { KnowledgeItem } from "../types";

export class PDFParser {
  static async parse(filePath: string): Promise<KnowledgeItem[]> {
    return []; // Stub until we implement
  }
}
